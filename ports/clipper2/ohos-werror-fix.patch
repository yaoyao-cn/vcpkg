 CPP/CMakeLists.txt | 12 ++++++++++--
 1 file changed, 10 insertions(+), 2 deletions(-)

diff --git a/CPP/CMakeLists.txt b/CPP/CMakeLists.txt
index 98d8ec4..618e96c 100644
--- a/CPP/CMakeLists.txt
+++ b/CPP/CMakeLists.txt
@@ -63,7 +63,11 @@ if (NOT (CLIPPER2_USINGZ STREQUAL "ONLY"))
   if (MSVC)
     target_compile_options(Clipper2 PRIVATE /W4 /WX)
   else()
-    target_compile_options(Clipper2 PRIVATE -Wall -Wextra -Wpedantic -Werror)
+    if("${CMAKE_SYSTEM_NAME}" STREQUAL "OHOS")
+      target_compile_options(Clipper2 PRIVATE -Wall -Wextra -Wpedantic)
+    else()
+      target_compile_options(Clipper2 PRIVATE -Wall -Wextra -Wpedantic -Werror)
+    endif()
     target_link_libraries(Clipper2 PUBLIC -lm)
   endif()
 endif()
@@ -85,7 +89,11 @@ if (NOT (CLIPPER2_USINGZ STREQUAL "OFF"))
   if (MSVC)
     target_compile_options(Clipper2Z PRIVATE /W4 /WX)
   else()
-    target_compile_options(Clipper2Z PRIVATE -Wall -Wextra -Wpedantic -Werror)
+    if("${CMAKE_SYSTEM_NAME}" STREQUAL "OHOS")
+      target_compile_options(Clipper2Z PRIVATE -Wall -Wextra -Wpedantic)
+    else()
+      target_compile_options(Clipper2Z PRIVATE -Wall -Wextra -Wpedantic -Werror)
+    endif()
     target_link_libraries(Clipper2Z PUBLIC -lm)
   endif()
 endif()
 